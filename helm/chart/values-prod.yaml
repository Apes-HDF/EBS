imagePullSecrets:
  - name: regcred

route:
  enabled: true
  host: plateforme-ebs.lt-mut-prod-scw.les-tilleuls.solutions

meilisearch:
  persistence:
    enabled: true
    storageClass: "sbs-default"
    size: "1Gi"

redis:
  persistence:
    enabled: true
    storageClass: "sbs-default"
    size: "1Gi"

php:
  fixtureJob:
    enabled: false
  fixtureCron:
    enabled: false
