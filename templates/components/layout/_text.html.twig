<div class="row {{ margin|default('mt-3') }} justify-content-center">
    <div class="{{ colClass|default('col-md-8 col-lg-6 col-xl-5') }}">
        <p class="fs-6 {{ font|default('fw-lighter') }} {{ textAlign|default('text-justify') }}">{{ text|raw|nl2br }}</p>
    </div>
</div>
